neo4j {
    # Default values for local development
    url = "bolt://localhost:7687"
    username = "neo4j"
    password = "password"
    
    # These values can be overridden by environment variables:
    # NEO4J_URL
    # NEO4J_USERNAME
    # NEO4J_PASSWORD
    
    # Connection settings
    batch-size = 1000
    timeout-seconds = 300
}

# You can add other application configurations here 